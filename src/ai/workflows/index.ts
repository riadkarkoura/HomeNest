/**
 * Barrel export for the AI workflow contract.
 *
 * No orchestrator is implemented here — only the shape of a workflow, its
 * steps, and the executor contract a future orchestration engine will
 * satisfy.
 */
export * from "./types";
